{
  "name": "polka",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Полка — своя библиотека документов и читалка к ним: EPUB, FB2, PDF, DOCX, TXT, Markdown.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/server.js",
    "dev": "node --watch server/server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.117.1",
    "pdfjs-dist": "^4.10.38"
  }
}
